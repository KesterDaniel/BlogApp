<%- include ("partials/header") %>

<h1>Index route</h1>

<% allBlogs.forEach((blog)=>{ %>
    <h1><%= blog.title %></h1>
    <img src="<%= blog.image %>">
    <p><%= blog.body %></p>
<% }) %>

<%- include("partials/footer") %>